# PyTorch CPU wheels. The default PyPI build on Linux bundles CUDA (~2 GB) and
# this app never touches a GPU, so Linux installs pin the +cpu local version.
# macOS wheels have no CUDA variant, so they use the plain version.
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.13.0+cpu ; sys_platform == "linux"
torch==2.13.0 ; sys_platform != "linux"
torchvision==0.28.0+cpu ; sys_platform == "linux"
torchvision==0.28.0 ; sys_platform != "linux"

flask==3.1.3
flask-cors==6.0.5
flask-limiter==4.1.1
redis==8.1.0
pillow==12.2.0
gunicorn==26.0.0
